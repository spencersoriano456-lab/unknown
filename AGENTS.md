# AGENTS.md

## What this repository contains
- A minimal repository with only a `README.md`.
- The README currently says "unknown" and "Flexcity", with no source files or configuration.

## Agent guidance
- Treat this as an early-stage or placeholder repo.
- Do not assume a language, framework, or build system unless the user specifies one.
- If asked to implement features, ask the user for:
  - the target language or engine
  - the intended platform (game, simulation, service, etc.)
  - build/test commands
  - file structure and conventions

## Focus for `Gun auto head`
- The phrase `Gun auto head` suggests a feature or project area around automatic gun targeting or headshot mechanics.
- Before coding, confirm with the user whether this is:
  - a gameplay mechanic
  - an auto-aim / aim-assist system
  - a simulation or robotics control feature
  - something else entirely

## If you need context
- Use `README.md` only as a starting point.
- If there is no code, ask the user to provide the first source file or a more detailed project description.
- Prefer minimal scaffolding and incremental changes rather than guessing a full architecture.

## Why this helps
- Provides an AI agent with clear behavior when the repository has little structure.
- Prevents incorrect assumptions and encourages clarification before implementation.
- Keeps the focus on the user's requested area: `Gun auto head`.
